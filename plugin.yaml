name: screen-awareness
manifest_version: 1
version: "1.1.2"
description: >
  /screen-awareness — capture selected monitor(s) before each API call and inject
  ephemeral vision context into the current turn.
author: SkyLaTry
kind: standalone
provides_hooks:
  - pre_api_request
